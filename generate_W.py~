import numpy as np


def usage():
    print "num_nodes behav_ft"
    sys.exit(2)


def generate_w(num_nodes, behav_ft):
    G=np.loadtxt('adj_mat',delimiter=',')
    Wd=np.random.poisson(lam=1,size=(num_nodes,num_nodes,behav_ft+2))
    a[a.nonzero()]=numpy.random.randn(len(a[a.nonzero()]))

def main():
    if len(sys.argv)<2:
        usage()
    
    num_nodes=int(sys.argv[1])
    behav_ft=sys.argv[2]
    generate_w(num_nodes,behav_ft)
    
if __name__ == '__main__':
    main()
